
@model LeadManagementSystem.MODEL.LeadOwnerModel

@if (Model.LeadOwnerDetails.Count < 0 || Model.LeadOwnerDetails != null)
{

    <table class="table" id="TableLeadSource">
        <thead>
            <tr>
                <th class="text-center">Sr.No</th>
                <th class="text-center">Owner Name</th>
                <th class="text-center" id="colAction">Action</th>
            </tr>
        </thead>
        <tbody class="text-center">
            @for (int i = 0; i < Model.LeadOwnerDetails.Count; i++)
            {
                <tr>
                    <td>@Model.LeadOwnerDetails[i].RowNum</td>
                    <td>@Model.LeadOwnerDetails[i].Owner_Name</td>

                    <td style="padding: 3px;">
                        <div class="btn-group">

                            <a data-toggle="tooltip" title="Update" data-placement="left" class="btn btn-sm waves-effect" style="padding-left: 3px; padding-right: 3px; width: 25px; height: 29px;"><i class="fa fa-edit" onclick="EditLeadOwner(@Model.LeadOwnerDetails[i].Owner_Id);"></i></a>
                            <a data-toggle="tooltip" title="delete" data-placement="left" class="btn btn-sm waves-effect" style="padding-left: 3px; padding-right: 3px; width: 25px; height: 29px;"><i class="fa fa-trash" onclick="RemoveLeadOwner(@Model.LeadOwnerDetails[i].Owner_Id);"></i></a>
                        </div>
                    </td>
                </tr>
            }
        </tbody>
    </table>
}
else
{
    <table class="table" id="TableLeadSource">
        <thead>
            <tr>
                <th class="text-center">Sr.No</th>
                <th class="text-center">Owner Name</th>
                <th class="text-center" id="colAction">Action</th>
            </tr>
        </thead>
        <tbody class="text-center">
            <tr>
                <td class="text-center">No data Found</td>
            </tr>
        </tbody>
    </table>
}
